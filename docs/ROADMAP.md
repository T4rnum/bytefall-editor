# Дорожная карта

Правило: каждый слой заканчивается работающей вещью, которой можно пользоваться. Следующий слой не
начинается, пока предыдущий не закоммичен с тестами и `npm run check` не зелёный.

Слои определены через «что можно делать в конце», а не через календарь. Решения, на которых они
стоят, — в [DESIGN.md](DESIGN.md).

---

## Слой 0. Гигиена — готово

- [x] Отдельный репозиторий, код перенесён из прототипа BlendPhoto.
- [x] Node 24 зафиксирован в `.nvmrc`, нижняя граница 22.12 в `engines`.
- [x] `npm run check` — одна команда, проверяющая всё, что проверяет CI.
- [x] GitHub Actions: typecheck, lint, формат, тесты, сборка на Linux и Windows.
- [x] Документация приведена в соответствие с реальным состоянием кода.
- [x] Фикстуры `.bp.json` всех поддерживаемых версий формата, записаны руками и заморожены.
- [x] Золотые снимки композитора и текстового экспорта, включая детерминизм эффектов по времени.
- [x] Бенчмарк-гарнитура на CPU: 256×144, 512×288 и разреженный 1024×1024, база зафиксирована
      в [BENCHMARKS.md](BENCHMARKS.md).

Пиксельные снимки PNG сюда не вошли: путь до картинки идёт через WebGL, которого в node нет.
Браузерная гарнитура появится вместе со слоем 2, где она и нужна.

**Готово:** свежий клон проходит `npm ci && npm run check`, CI зелёный, документация не заявляет
несуществующую функциональность.

---

## Слой 1. Фундамент интерфейса — готово

- [x] Полный набор токенов в `styles/tokens.css`: поверхности, границы, текст, состояния, отступы,
      радиусы, тени, типографика, размеры контролов, движение. Тема меняется заменой одного блока.
- [x] Базовые контролы в `app/ui`: кнопка, драг-число в стиле Blender, ползунок с точным вводом,
      текстовое поле с отложенной записью, флажок, выпадающий список, вкладки, поле с подписью,
      сворачиваемая панель, разделитель областей.
- [x] Числовая логика драг-поля вынесена в `ui/numeric.ts` и покрыта тестами без браузера.
- [x] Раскладка: сворачивание панелей и ширина сайдбара запоминаются между сессиями.
- [x] Горячие клавиши в одном реестре, справка строится из него же и открывается по `?`.
- [x] Все панели переведены на новые контролы, старые ad-hoc классы удалены.
- [x] Сочетания клавиш не зависят от раскладки: буквы и цифры сверяются по физической клавише.
- [x] Свой ползунок вместо `input[type=range]`: нативный не знает про Shift и Ctrl.
- [x] Плавный переход камеры для дискретных действий: кнопки зума и подгонка под окно.
- [x] Цветовой образец отдельным контролом: выбор цвета, сброс в прозрачность, системная пипетка
      там, где браузер её поддерживает.
- [x] Свои всплывающие подсказки вместо `title`: задержка настраивается, по клавиатуре подсказка
      появляется сразу.

**Готово:** все панели выглядят единообразно, тема меняется одним набором переменных, в
компонентах нет захардкоженных цветов.

---

## Слой 2. Рендер: тайлы и грязные области — готово

- [x] Тайловая раскладка `core/tiles.ts`: тайлы 32×32, слоты без потерь на обрезанных краях,
      склейка соседних тайлов в непрерывные диапазоны.
- [x] Тайл-мажорный порядок инстансов, чтобы тайл был непрерывным диапазоном в буфере.
- [x] Грязные тайлы вычисляются из `CellEdits`.
- [x] Частичная заливка GPU только по грязным диапазонам: во время штриха на GPU уходит
      один-два тайла вместо всего холста.
- [x] Индекс ячеек по тайлам и частичная пересборка кадра: мазок пересобирает один-два тайла
      вместо всего холста. Равенство с полной пересборкой закреплено 12 тестами.
- [x] Грязные тайлы для коммита, а не только для превью штриха. Структурные правки, смена кадра
      и отмена по-прежнему честно требуют полной пересборки.
- [x] Кэш эффектов по подписи времени и тик, не меняющий картинку, не доходит до композитора.
- [x] Восстановление после потери WebGL-контекста, с сообщением пользователю.
- [x] Браузерная гарнитура на собранной версии: заливка, отрисовка, полный шаг рисования и
      девять проверок того, что попало на экран.
- [x] Culling тайлов по вьюпорту — **не нужен**. Отрисовка миллиона инстансов с ожиданием GPU
      стоит 0.05 мс, экономить нечего. Решено по измерению, а не по предположению.

**Готово:** одиночный мазок не трогает весь холст, полный шаг рисования стоит 0.15–0.61 мс, то есть
целевые 60 и 30 кадров в секунду выполняются с запасом больше чем в двадцать раз. Числа — в
[BENCHMARKS.md](BENCHMARKS.md).

---

## Слой 3. 2D-редактор до надёжного состояния

- [x] Лассо и волшебная палочка: выделение стало произвольной маской ячеек, `Shift` добавляет,
      `Alt` вычитает.
- [x] Рисование только внутри выделения: правки обрезаются маской в `ToolEnv`, поэтому правило
      одно на все инструменты сразу.
- [x] Рисование на ЛКМ и ПКМ разными символами и цветами: две кисти, `B` переключает, `Shift+X`
      меняет местами, пипетка правит кисть нажатой кнопки.
- [x] Диалог изменения размера холста: якорь из девяти положений и предпросмотр обрезки,
      `Ctrl+Alt+C`.
- [x] Шахматная подложка для прозрачного холста: клетка на ячейку, `~` выключает, в экспорт
      не попадает.
- [x] Миниатюры кадров в таймлайне: пересчитывается только изменившийся кадр, в паузе между
      правками.
- [x] Копирование и вставка объектов, перенос объекта на другой слой: вставка встаёт на то же
      место активного слоя, `Alt+]` и `Alt+[` перекладывают объект между слоями.
- [x] Свойства ячейки (`attrs`) в инспекторе: правка у всех выделенных сразу, поиск ячеек по
      свойству, `Alt+Enter` ведёт к полю нового свойства.
- [x] Автосохранение и восстановление после аварийного закрытия: IndexedDB, запись на вкладку,
      живые вкладки видны через Web Locks.
- [x] Тесты инструментов через `ToolEnv` без браузера (`src/app/tools/__tests__/testEnv.ts`).
- [x] Импорт `.xp` из REXPaint и старых файлов первого прототипа: через «Открыть», формат
      узнаётся по содержимому, импорт к исходному файлу не привязывается.

**Готово, когда:** редактором можно пользоваться для 2D-работы целый день, все основные операции
защищены undo/redo, закрытие вкладки не приводит к тихой потере работы.

---

## Слой 4. Трансформации объектов

Первое появление инстансного пути рендера, см.
[DESIGN.md, раздел 2](DESIGN.md#2-главный-инвариант-где-живут-дробные-координаты).

- [x] `Transform2D` на объекте: поворот, масштаб, pivot, ограниченный визуальный сдвиг.
- [x] `InstanceMesh` рядом с `GridMesh`, отдельный проход для инстансных слоёв.
- [x] Гизмо трансформации во вьюпорте.
- [x] `overrides`: поворот и размер отдельного выбранного символа или группы символов.
- [x] `rasterize()` — общая функция для экспорта в текст и команды «впечатать в слой».
- [x] Иерархия: `parentId`, группировка объектов, наследование трансформа.
- [x] Формат файла: новая версия с миграцией.

**Готово, когда:** группу символов можно повернуть и отмасштабировать, отдельному символу можно
задать свой поворот и размер, экспорт в текст по-прежнему однозначен.

---

## Слой 5. Конвертер изображений

- [x] Модуль `core/quantize`: рампы, веса яркости, гамма, дизеринг, детекция краёв, палитры.
- [x] Диалог импорта с живым предпросмотром и всеми параметрами.
- [x] Перетаскивание изображения в окно.
- [x] Золотые тесты конвертации.

**Готово, когда:** картинка перетаскивается в окно и превращается в символы с настраиваемым видом.

---

## Слой 6. Время и ключевые кадры — готово

- [x] Таймлайн в секундах, глобальное время.
- [x] Покадровая анимация мигрирует в спрайт-трек без потери данных: кадры и есть спрайт-трек.
- [x] Типизированные треки, интерполяция `step` / `linear` / `bezier`.
- [x] Треки позиции, поворота, масштаба, непрозрачности, цвета (оттенок объекта).
- [x] Ключи параметров эффектов.
- [x] Выделение, перемещение и привязка ключей в таймлайне.
- [x] Единый `evaluate(t)` для вьюпорта и экспорта, детерминированные тесты экспорта.
- [ ] Редактор кривых: свои ручки вместо готовых характеров.
- [ ] Миниатюры кадров с учётом треков, калька по времени, а не только по кадрам.

**Готово, когда:** объект плавно движется и меняется между ключами, перемотка в любой момент
детерминирована, GIF совпадает с экраном.

---

## Слой 7. Деформеры — готово

- [x] Стек деформеров на объекте, порядок важен. Правка стека идёт во все кадры объекта.
- [x] Первый набор: `Wave`, `Jitter`, `Twist`, `ScaleFalloff`, `ColorRamp`. В текст
      деформированный объект уходит прямым путём: символ — в ячейку под своим центром.
- [x] Параметры деформеров анимируются треками.
- [x] GPU-материалы на объекте: контур и свечение. Оттенок и градиент уже есть на CPU — оттенок
      объекта и деформер «Градиент» — и уходят в текст вместе с цветами.
- [x] GPU-материалы: блик и дизеринг. Пульс яркости уже есть эффектом слоя и ключами
      оттенка и непрозрачности, поэтому вместо него — бегущий блик по пикселям символов.
- [x] `Bend`, `Explode`, `GlyphRamp`: изгиб, разлёт и символы по яркости.
- [x] Частицы, рисующие символы: вид деформера без симуляции. Частица номер i рождается в
      момент i / rate, остальное берётся из шума по номеру и зерну, поэтому перемотка
      детерминирована и экспорт совпадает с экраном.

**Готово, когда:** группу символов можно заставить волноваться, дрожать и переливаться без ручной
расстановки ключей на каждый символ.

---

## Слой 8. Связи и риг

Кинематика без физики, см. [DESIGN.md, раздел 4.4](DESIGN.md#44-связи-вместо-физики).

- [ ] Кости, поза покоя, иерархия.
- [ ] Автопривязка весов по расстоянию, позже кисть весов.
- [ ] Скиннинг как деформер.
- [ ] IK-цепь на FABRIK с ограничением углов.
- [ ] Цепь с задержкой и слежение — верёвочный вид.
- [ ] Контроллеры и гизмо рига во вьюпорте.

**Готово, когда:** персонаж из символов машет рукой через контроллер, цепочка объектов идёт за
ведущим как верёвка, и результат одинаков при перемотке и в экспорте.

---

## Слой 9. Экспорт в игровые движки

- [ ] Спрайтшит с JSON-атласом и PNG-секвенция.
- [ ] Формат `.bytefall`: атлас плюс поток инстансов на кадр.
- [ ] Рантайм для Godot 4.
- [ ] Рантайм для Unity.

**Готово, когда:** анимация из редактора крутится в пустом проекте Godot.

---

## Слой 10. Псевдо-3D, режим A

- [ ] `Scene3D` в документе: примитивы, камера, ambient и directional свет.
- [ ] Offscreen render target, цвет плюс глубина плюс нормали.
- [ ] Квантайзер из слоя 5 превращает результат в слой ячеек.
- [ ] Импорт glTF только на чтение.
- [ ] Ключи трансформа объектов и камеры.

**Готово, когда:** импортированная модель крутится и выглядит символьным рисунком, экспорт
совпадает с экраном.

---

## Слой 11. Псевдо-3D, режим B

- [ ] Глифы с нормалями в пространстве, сэмплирование поверхности меша.
- [ ] Освещение на глиф, выбор символа по яркости из рампы.
- [ ] Туман разрежением по рампе, размер по глубине.
- [ ] Билборды, подрежимы «привязан к экранной сетке» и «свободный».
- [ ] Пикинг и выделение в 3D, LOD и culling.

**Готово, когда:** сфера из символов освещается движущимся источником света, камера свободно летает
вокруг, и сцена остаётся интерактивной.

---

## Слой 12. Настраиваемое рабочее место

Ориентир — VS Code: панель можно перетащить в другую зону, вынести в отдельное окно, скрыть.
Это не косметика: у рисования, анимации и работы с 3D разные наборы нужных панелей, и раскладка
под одну задачу мешает другой.

- [ ] Панель как самостоятельная сущность с идентификатором, а не жёстко вписанная в сайдбар.
- [ ] Зоны размещения: левая и правая колонки, нижняя полоса, вкладки внутри зоны.
- [ ] Перетаскивание панели между зонами с подсветкой места вставки.
- [ ] Сохранение раскладки и готовые наборы под задачу: рисование, анимация, 3D.
- [ ] Отдельное окно для панели.
- [ ] Редактор тем поверх токенов из `styles/tokens.css`.

**Готово, когда:** пользователь собирает рабочее место под себя, раскладка переживает
перезагрузку и переключается между наборами одним действием.

Отдельные предпосылки уже заложены: панели сворачиваются и запоминают состояние, ширина сайдбара
тянется, всё оформление идёт через токены. Полноценный докинг требует, чтобы панели перестали быть
жёстко перечисленными в `App.tsx`, — это и есть основная работа слоя.

---

## Слой 13. Продукт

- [ ] Tauri 2: нативные диалоги, недавние проекты, вотчинг файлов.
- [ ] Пользовательские шрифты, атлас с неквадратными ячейками.
- [ ] Отменяемый экспорт, фоновые задачи в воркерах.
- [ ] Логирование ошибок без содержимого документа.

---

## Бэклог из проверки пользователем

- [x] Кнопка сброса у каждого параметра: появляется рядом, когда значение отличается от
      значения по умолчанию. То же в окне импорта картинки.
- [x] Перетаскивание слоёв мышью, а не только кнопками.
- [x] Числовые поля с перетаскиванием вместо ползунков: значение видно, места меньше.
- [ ] Свои палитры в импорте картинки.
- [x] Двигать и приближать холст, пока открыт импорт картинки.
- [x] Вкладки символов и кнопки панелей не помещались в сайдбар и давали прокрутку вбок.
- [ ] Общая переработка интерфейса: сейчас он не интуитивный, начинать с неё слой 12.

## Идеи на потом

- **3D: холст на поверхности модели.** Импорт модели, сетка ячеек обёрнута вокруг неё как
  развёртка текстуры, рисовать можно по развёртке и прямо по полигонам. Символы лежат на
  поверхности текстурой или смотрят в камеру. Ложится на режимы B и C из DESIGN.md, раздел 6;
  в движок режим «текстурой» уходит обычной моделью с атласом, режим «в камеру» — рантаймом
  слоя 9.
- **Интеграция с ИИ.** MCP-сервер поверх ядра: прочитать область документа текстом с цветами,
  записать ячейки, создать объект, поставить ключ, отрендерить превью, отменить. Ядро чистое,
  поэтому сервер работает и без браузера, с файлом `.bp.json`.

## Сквозное через все слои

- Золотые тесты «экран равен экспорту» на каждую новую возможность рендера.
- Бенчмарки в репозитории, а не в устных оценках.
- Миграция схемы и фикстура старой версии на каждое изменение формата.
- Каждая фича получает место в интерфейсе и горячую клавишу до того, как считается готовой.
