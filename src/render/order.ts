/**
 * Порядок отрисовки сцены. Снизу подложка холста, над ней проходы кадра по порядку — сетка
 * ячеек и потоки свободных символов вперемешку, — и поверх всего служебная графика. Проходов
 * бывает много, поэтому у служебной графики номер с большим запасом.
 */
export const RENDER_ORDER = {
  backdrop: 0,
  /** Номер первого прохода кадра, следующие идут подряд. */
  content: 1,
  gridLines: 1_000_000,
  marks: 1_000_001,
  cursor: 1_000_002,
} as const;
